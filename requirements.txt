# Core dependencies
python-docx>=0.8.11
tk>=0.1.0  # Tkinter GUI support

# Windows-specific dependencies
pywin32>=306; platform_system == "Windows"

# macOS-specific dependencies
pyobjc-framework-Cocoa>=9.2; platform_system == "Darwin"
pyobjc-framework-CoreServices>=9.2; platform_system == "Darwin"
pyobjc-framework-ScriptingBridge>=9.2; platform_system == "Darwin"

# Linux-specific dependencies (Oracle Linux 7/8/9, Ubuntu LTS)
pypandoc>=1.11; platform_system == "Linux"  # Python interface for Pandoc
